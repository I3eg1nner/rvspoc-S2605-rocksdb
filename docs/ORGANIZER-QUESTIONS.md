# 致主办方的澄清问题（S2605，提交前）

1. **+30% 的聚合口径**：fillrandom/readrandom/seekrandom 三项
   （a）各自 ≥30%，（b）综合/平均 ≥30%，还是（c）任一 ≥30%？
   线程数与 db_bench 参数是否有官方固定值？
2. **基线定义**：对照基线是（a）同工具链同优化级别的纯标量构建，
   还是（b）仓库默认构建？通用编译优化（-O3、PGO、LTO）产生的增益
   是否计入"RVV 优化"的差值？（我们的三臂对照显示该口径对结论影响
   决定性，详见 PR 内 ACCEPTANCE 二节。）
3. **评测环境**：能否提供 LX5000 的访问或其 isa 字符串
   （zbc/zvbc 是否存在直接决定 CRC 阶梯落在哪一层）与评测工具链
   （gcc/clang 版本）？
4. **RocketMQ P99**：官方 P99 以何工具/口径测量？RocketMQ 自带
   benchmark 不输出分位数。
5. **PR 更新截止**：08-31 AoE 前开出的 PR，其后的 commit 是否计入？
